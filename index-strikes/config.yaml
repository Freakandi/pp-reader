name: "Index Strikes"
version: "0.1.36"
slug: index-strikes
description: "Stock index options strike price recommendations based on historical volatility analysis"
url: "https://github.com/Freakandi/index-strikes"

arch:
  - aarch64
  - amd64

startup: application
boot: auto

init: false
ingress: true
ingress_port: 8099
panel_icon: "mdi:chart-line"
panel_title: "Index Strikes"

options:
  db_host: "192.168.1.100"
  db_port: 5432
  db_name: "index_strikes"
  db_user: "index_strikes"
  db_password: ""
  update_schedule: "16:00"
  log_level: "info"
  timezone: ""

schema:
  db_host: str
  db_port: port
  db_name: str
  db_user: str
  db_password: password
  update_schedule: str?
  log_level: list(debug|info|warning|error)?
  timezone: str?

image: "ghcr.io/freakandi/index-strikes/{arch}"
